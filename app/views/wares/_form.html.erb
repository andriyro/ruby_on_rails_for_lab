<%= form_with(model: ware) do |form| %>
  <% if ware.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(ware.errors.count, "error") %> prohibited this ware from being saved:</h2>

      <ul>
        <% ware.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name_product, style: "display: block" %>
    <%= form.text_field :name_product %>
  </div>

  <div>
    <%= form.label :price, style: "display: block" %>
    <%= form.text_field :price %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
