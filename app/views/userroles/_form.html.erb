<%= form_with(model: userrole) do |form| %>
  <% if userrole.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(userrole.errors.count, "error") %> prohibited this userrole from being saved:</h2>

      <ul>
        <% userrole.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :namerole, style: "display: block" %>
    <%= form.text_field :namerole %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
