<%= form_with(model: cart) do |form| %>
  <% if cart.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(cart.errors.count, "error") %> prohibited this cart from being saved:</h2>

      <ul>
        <% cart.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :namecart, style: "display: block" %>
    <%= form.text_field :namecart %>
  </div>

  <div>
    <%= form.label :userid, style: "display: block" %>
    <%= form.number_field :userid %>
  </div>

  <div>
    <%= form.label :status, style: "display: block" %>
    <%= form.text_field :status %>
  </div>

  <div>
    <%= form.label :creation_timestamp, style: "display: block" %>
    <%= form.datetime_field :creation_timestamp %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
